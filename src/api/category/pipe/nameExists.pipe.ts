// import { Injectable, PipeTransform, ArgumentMetadata } from "@nestjs/common";

// @Injectable()

// export class NameExists implements PipeTransform {
//     transform(value: object, metadata: ArgumentMetadata) {
//         return value;
//     }
// }